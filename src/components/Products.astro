---
import { Icon } from 'astro-icon/components';
import '../styles/global.css';
import { products } from './products';
---

<section id="products">
  <div class="mx-auto max-w-screen-xl px-4 py-8 sm:px-6 sm:py-12 lg:px-8">
    <div class="flex flex-col gap-3 mx-auto">
      <h1 class="text-2xl font-bold text-gray-900 sm:text-3xl text-center w-full mt-12">Productos</h1>
      <p class="mx-auto text-gray-500 text-center px-3 md:px-10 lg:px-30 text-sm sm:text-base">
        Explora nuestra selección, elige tus favoritos y nosotros nos encargamos del resto. ¡Así de simple!
      </p>
    </div>

    <ul class="mt-8 grid gap-4 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4">
      {products.map((product) => (
        <li class="group block overflow-hidden relative">
          <div class="flex flex-col gap-4 rounded-2xl p-4 bg-linear-to-t from-sky-100 to-sky-50">
            <img
              src={product.image}
              alt=""
              class="h-[200px] w-full object-cover transition duration-500 group-hover:scale-103 rounded-2xl"
            />

            <div class="flex flex-col gap-4 justify-between px-2">
              <h3 class="text-1xl text-black font-semibold group-hover:underline group-hover:underline-offset-4">
                {product.name}
              </h3>
              <div class="flex gap-7">
                <div class="flex items-center gap-2">
                  <Icon name="price" class="size-5" />
                  <span class="text-gray-600 text-sm">${product.price.toFixed(2)}</span>
                </div>

                <div class="flex items-center gap-2">
                  <Icon name={product.storeIcon} class="size-5" />
                  <span class="text-gray-600 text-sm">{product.store}</span>
                </div>
              </div>
              <button class="flex justify-center items-center size-11 p-3 rounded-full cursor-pointer bg-white/80 hover:bg-white transition absolute top-7 right-7">
                <Icon name="cart" class="size-full" />
              </button>
            </div>
          </div>
        </li>
      ))}
    </ul>
  </div>
</section>
